## Helm Chart for mcs_vshk_certification

# Run from this folder:

```console
helm install mcs_vshk_certification --set databaseHost="<my.postgresql.host>" --set databaseUser=<user> --set databasePassword=<password>  .
```
